- createTab.jsx needs some css
- The user has an issue including their full name in the (name) box. Immediately gets moved after the first letter is clicked (guest view). 

- App.jsx should be the core. 

- Host should have a QR code available for scanning that takes the user to the "tab/:id"

- Styling is a bit inconsistent. three.js is not being utilized in styling for some reason. Maybe inconsistency is a structural issue.a